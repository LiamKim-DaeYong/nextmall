spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nextmall
    username: nextmall
    password: nextmall1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
