token:
  passport:
    secret-key: ${TOKEN_PASSPORT_SECRET}

nextmall:
  identifier:
    node-id: 1

logging:
  level:
    org.springframework.security: DEBUG
    com.nextmall.common.authorization: DEBUG
