databaseChangeLog:
  - changeSet:
      id: 0003-create-auth-user-account-table
      author: Liamkim
      changes:
        - createTable:
            tableName: auth_user_accounts
            columns:
              - column:
                  name: id
                  type: BIGINT
                  constraints:
                    primaryKey: true
                    nullable: false

              - column:
                  name: user_id
                  type: BIGINT
                  constraints:
                    nullable: false

              - column:
                  name: provider
                  type: VARCHAR(50)
                  constraints:
                    nullable: false

              - column:
                  name: provider_account_id
                  type: VARCHAR(255)
                  constraints:
                    nullable: false

              - column:
                  name: password_hash
                  type: VARCHAR(255)
                  constraints:
                    nullable: true

              - column:
                  name: created_at
                  type: TIMESTAMP WITH TIME ZONE
                  constraints:
                    nullable: false

              - column:
                  name: updated_at
                  type: TIMESTAMP WITH TIME ZONE
                  constraints:
                    nullable: false

        - addUniqueConstraint:
            tableName: auth_user_accounts
            columnNames: provider, provider_account_id
            constraintName: uk_auth_user_accounts_provider_account_id

        - addForeignKeyConstraint:
            baseTableName: auth_user_accounts
            baseColumnNames: user_id
            referencedTableName: users
            referencedColumnNames: id
            constraintName: fk_auth_user_accounts_user_id
            onDelete: CASCADE
